import WarningModel from 'bundle-loader?lazy!./warning';

let WarningRoutes = [
    {
        path: '/page/qinpu/warning',
        component: WarningModel
    }
]
WarningRoutes=WarningRoutes.concat();

export default WarningRoutes;